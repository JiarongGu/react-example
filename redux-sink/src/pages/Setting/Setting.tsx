import * as React from 'react';

import { RouteContent } from '@components/RouteContent';

export const Setting = () => {
  return (
    <div>
      <RouteContent routeKey={'setting'} />
    </div>
  )
}